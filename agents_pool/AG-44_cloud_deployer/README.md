# AG-44 Cloud Deployer (stub v1)

Estado: diseño + `cloud_deploy_plan` + `get_development_roadmap`.

Próximo: Terraform Cloud Run + gate humano para `apply`.

Coordinación AntiGravit: FEMAR `femar-mvp-core` Cloud Run.
