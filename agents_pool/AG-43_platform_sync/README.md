# AG-43 Platform Sync

- `sync_platform_to_intel()` — rsync platform AMD→Intel + restart MCP (desde AMD)
- `run_failover_dry_run()` — script `scripts/failover_intel_to_amd.sh`
- `clone_tenant_deployment(slug, entity_id)` — `scripts/clone_deployment.sh`
