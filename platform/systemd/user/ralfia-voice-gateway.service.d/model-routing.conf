[Service]
# Voice runs on Intel .4, while the resident ROCm10 vLLM model runs on AMD .5.
# Load this after the generic platform .env so voice cannot be sent back to a
# stale local Intel :8000 endpoint after reboot or service restart.
EnvironmentFile=-/home/rlopez/inneros/inneros_core/platform/config/voice-gateway.env
