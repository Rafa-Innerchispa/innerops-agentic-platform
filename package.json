{
  "name": "inneros-agentic-platform",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
